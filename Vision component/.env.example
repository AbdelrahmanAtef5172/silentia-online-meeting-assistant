# Environment configuration example
# Note: ENV=development disables model warmup (warmup_on_init: false).
#       For production or testing with warmup, set ENV=production or unset ENV.
ENV=development
# CUDA_VISIBLE_DEVICES=0
